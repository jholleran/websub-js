'use strict'

test('Discover the Hub and Topic given a resources URL', () => {
});

test('Subscribe to updates at the hub', () => {
});

test('Accepts content distribution requests from the hub', () => {
});